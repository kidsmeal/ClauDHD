{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "main window: core defaults only, app commands need no grants",
  "windows": ["main"],
  "permissions": ["core:default"]
}
