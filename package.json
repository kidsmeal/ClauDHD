{
  "name": "claudhd-dev",
  "private": true,
  "description": "Dev tooling (tests) for the ClauDHD plugin. Not published and not part of the installed plugin, which lives in plugins/claudhd and has zero dependencies.",
  "scripts": {
    "test": "node --test"
  }
}
